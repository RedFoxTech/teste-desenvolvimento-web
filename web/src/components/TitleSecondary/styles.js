import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
  title: {
    fontSize: '3rem',
    fontWeight: 600,
    marginBottom: '3rem',
    textAlign: 'center'
  }
}))

export default useStyles;